int z = 3;
int sa= (int)'a';
int x = 1;
int y = 4;
char q = 'v';
const int broj = 5;
const char r = (const char)z;
const int pa = (const int)'a';
char t = (char)((const int)300 );
int p = (int)(char)(const int)(const char)(x + y);
int fun(int q, int p , int a)
{
	int pa = q + p + a + 'p';
	p = fun(1,2,3);
	return pa;
}
int main(void) {
 int x = q + 5;
 int y = 6 + z; // greska
 y = x + 2;
 //return fun(x,y,x+y);
 return 0;
 }
